# Địa chỉ API của backend.
# - Để TRỐNG hoặc không có file này  → dùng server ĐÃ DEPLOY trên Azure (mặc định trong code).
# - Muốn dev với BE chạy máy local  → copy file này thành .env.local rồi bỏ comment dòng dưới:

# VITE_API_URL=http://localhost:5000/api

# Khi deploy FE lên host khác (Vercel/Netlify/Azure Static Web App) mà BE ở domain riêng:
# VITE_API_URL=https://hrms-backend-a4dwfmgmgfagf7ax.southeastasia-01.azurewebsites.net/api
